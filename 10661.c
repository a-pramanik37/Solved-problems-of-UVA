#include <stdio.h>
int main()
{
    long long int c[50005], q, i, n, Q, j, flg, b[25005];

    while(scanf("%lld", &n)==1){
        for(i = 1; i<= n; i++){
            scanf("%lld", &a[i]);
        }

        scanf("%lld", &Q);

        for(i = 1; i<= Q; i++){
            scanf("%lld", &b[i]);
        }

    }

    return 0;
}
