#include <stdio.h>
#include <string.h>
int main()
{
    int a[100], b[100], i, p;

    while(scanf("%d", &n)==1){
            if(n==0) break;

            memset(a, 0, sizeof a);
    printf("Discarded cards: ");
        for(i = 1, j = 1; i<=n; i++){
            if(i==1) printf("%d");
            else if(i%2==1) printf(", %d");
            else {
                a[j] = i;
                j++;
            }
        }
        if(n%2==0) a[j] = n;

        if(n%2==0){
            while{
            p = z;
            memset(b, 0, sizeof b);

            for(i = 1; i<=p; i++){

            }
        }
        }
    }
}
